Muuttaa game taulussa game.id tekemään automaattisesti id kun screen_name luodaan
ALTER TABLE goal_reached
DROP FOREIGN KEY goal_reached_ibfk_1;

ALTER TABLE game
MODIFY id INT NOT NULL AUTO_INCREMENT;

ALTER TABLE goal_reached
ADD CONSTRAINT goal_reached_ibfk_1
FOREIGN KEY (goal_id)
REFERENCES game(id);

Muttaa game taulussa, että pelaaja voi vain lisästä uniikin nimen
ALTER TABLE game
ADD UNIQUE (screen_name);

ALTER TABLE game CHANGE co2_consumed balance INT(11) DEFAULT 2000;
ALTER TABLE game CHANGE co2_budget flights INT(3) DEFAULT 0;
